# yard-agentdocs example — API Reference

Hand-authored target output for the `example/lib` worked example. See
`devdocs/DESIGN.md` for the design rationale behind this format.

## Top-level namespaces

- [`Geometry`](Geometry.md) — A small toy geometry namespace, used as a
  worked example for the `yard-agentdocs` output format.
