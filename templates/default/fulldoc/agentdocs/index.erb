---
okf_version: "0.1"
---

# <%= options.title %>

## Guides

* [`Navigating these docs`](navigating.md) - How to look up Ruby classes, modules, methods, and other members, and how to interpret entries in this knowledge bundle.
<%- options.files.each do |file| -%>
* [`<%= file.title %>`](file.<%= file.name %>.md)
<%- end -%>

## Classes & modules

<%- @indexed_objects.each do |object| -%>
<%- suffix = private_api_annotation_short(object) -%>
* [`<%= object.path %>`](<%= indexed_object_path(object) %>)<%= " (#{suffix})" if suffix %><%= index_summary_suffix(object) %>
<%- end -%>
