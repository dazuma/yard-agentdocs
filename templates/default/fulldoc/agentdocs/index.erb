# <%= options.title %>

## Top-level namespaces

<%- @top_level_objects.each do |object| -%>
- [`<%= object.name %>`](<%= top_level_object_path(object) %>) — <%= object.docstring.summary %>
<%- end -%>
